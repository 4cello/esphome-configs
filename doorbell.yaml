substitutions:
    devicename: doorbell

packages:
  wifi: !include common/wifi.yaml
  device_base: !include common/device_base.yaml

esphome:
  platform: ESP8266
  board: esp01_1m

captive_portal:

binary_sensor:
  - platform: gpio
    pin: GPIO2
    name: Doorbell
    device_class: sound
    filters:
      - invert:
      - delayed_off: 2s